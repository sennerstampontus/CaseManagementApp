<UserControl x:Class="CaseManagementApp.Views.CreateNewCustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CaseManagementApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Column="1" Grid.Row="0" Background="#ACB0BD">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <RadioButton Grid.Column="0" Style="{StaticResource ContentMenuButtons}"  Content="New Customer" Command="{Binding CreateCustomerCommand}" FontSize="14" IsChecked="True"/>
                <RadioButton Grid.Column="1" Style="{StaticResource ContentMenuButtons}"  Content="New Administrator" Command="{Binding CreateAdminCommand}" FontSize="14" />

            </Grid>
        </Grid>
    </Grid>
</UserControl>
